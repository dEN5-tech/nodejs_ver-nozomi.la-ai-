const DEBUG = false;
const START_EPOCH = 0;

const EXPLORE_PROB = 0.2;
const POOL_SIZE = 80;
const VIEW_RATIO = 0.2;
const ATTITUDE_TOWARDS_NOVEL_TAGS = 1;

const JOB_POOL_SIZE = 2;
const JSON_MAX = 4;
const PORT = 2314;
const HOST = "localhost";

const FILTER = "";

module.exports = {
  DEBUG,
  START_EPOCH,

  EXPLORE_PROB,
  POOL_SIZE,
  VIEW_RATIO,
  ATTITUDE_TOWARDS_NOVEL_TAGS,

  JOB_POOL_SIZE,
  JSON_MAX,
  PORT,
  HOST,

  FILTER,
};
